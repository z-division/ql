func AssignDemo() int {
    // type inferred from expression
    var x = 5

    // explicit type specification
    var y uint64 = 6.0

    // inherit type from original var statement
    x = 7

    const z = "constant"
}
